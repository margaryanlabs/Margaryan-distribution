import {NextResponse} from "next/server"; import {buildDistributionPlan} from "@/lib/agent/planner"; import type {MissionInput} from "@/lib/types";
export async function POST(request:Request){try{const body=await request.json() as MissionInput;if(!body.goal?.trim())return NextResponse.json({error:"goal is required"},{status:400});const plan=await buildDistributionPlan({goal:body.goal.trim(),market:body.market?.trim()||"Global",language:body.language==="ru"?"ru":"en",autonomy:["auto","draft"].includes(body.autonomy)?body.autonomy:"approve"});return NextResponse.json({plan});}catch(error){return NextResponse.json({error:error instanceof Error?error.message:"Planner failed"},{status:500});}}
