import {NextResponse} from "next/server"; import OpenAI from "openai";
export async function POST(req:Request){const{lead,product,language="en"}=await req.json();if(!process.env.OPENAI_API_KEY)return NextResponse.json({ok:false,error:"OPENAI_API_KEY missing"},{status:503});const openai=new OpenAI({apiKey:process.env.OPENAI_API_KEY});const r=await openai.responses.create({model:"gpt-5.6-terra",input:`Write one highly personalized LinkedIn B2B outreach draft in ${language==="ru"?"Russian":"English"}. 350 characters max. No hype, no fake familiarity, no invented facts. End with a low-friction question.\nLEAD: ${JSON.stringify(lead)}\nPRODUCT: ${JSON.stringify(product)}`});return NextResponse.json({ok:true,mode:"draft_only",requiresHumanSend:true,draft:r.output_text.trim()});}
