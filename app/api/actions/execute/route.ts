import {NextResponse} from "next/server"; import {evaluateExecution} from "@/lib/compliance"; import {executeProviderAction} from "@/lib/integrations"; import type {ExecuteRequest} from "@/lib/types";
export async function POST(request:Request){try{const action=await request.json() as ExecuteRequest;const policy=evaluateExecution(action);if(!policy.allowed)return NextResponse.json({state:"blocked",reason:policy.reason},{status:409});const result=await executeProviderAction(action);return NextResponse.json({state:"executed",result});}catch(error){return NextResponse.json({error:error instanceof Error?error.message:"Execution failed"},{status:500});}}
