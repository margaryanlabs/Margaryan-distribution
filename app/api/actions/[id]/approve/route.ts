import { NextResponse } from "next/server";
import { distributionStore } from "@/lib/store";
import { storageRuntime, withDurableState } from "@/lib/store/checkpoint";
export async function POST(_:Request,context:{params:Promise<{id:string}>}){try{const{id}=await context.params;const{result,persistence}=await withDurableState(()=>{const action=distributionStore.getAction(id);if(!action)return NextResponse.json({error:"Action not found"},{status:404});if(action.status!=="queued")return NextResponse.json({error:`Cannot approve action in ${action.status} state`},{status:409});const updated=distributionStore.updateAction(id,{mode:"AUTO",status:"approved",approvedAt:new Date().toISOString(),error:undefined});return NextResponse.json({action:updated});});result.headers.set("x-distribution-storage",storageRuntime().storage);result.headers.set("x-distribution-persisted",String(Boolean(persistence.saved)));return result;}catch(error){return NextResponse.json({error:error instanceof Error?error.message:"Approval failed",runtime:storageRuntime()},{status:500});}}
