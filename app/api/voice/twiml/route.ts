function xmlEscape(value:string){return value.replace(/&/g,"&amp;").replace(/</g,"&lt;").replace(/>/g,"&gt;").replace(/\"/g,"&quot;").replace(/'/g,"&apos;");}
export async function POST(request:Request){const url=new URL(request.url);const streamUrl=process.env.VOICE_GATEWAY_WSS_URL;if(!streamUrl)return new Response("VOICE_GATEWAY_WSS_URL missing",{status:500});const leadName=xmlEscape(url.searchParams.get("leadName")||"");const objective=xmlEscape(url.searchParams.get("objective")||"qualify interest");const xml=`<Response><Connect><Stream url="${xmlEscape(streamUrl)}"><Parameter name="leadName" value="${leadName}"/><Parameter name="objective" value="${objective}"/></Stream></Connect></Response>`;return new Response(xml,{headers:{"Content-Type":"text/xml; charset=utf-8"}});}
